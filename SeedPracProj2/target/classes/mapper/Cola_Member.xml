<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper/Cola_Member.xml">
<!-- 로그인처리 -->
   <select id="Cola_isMember" parameterType="java.util.Map" resultType="int">	
   SELECT count(*) FROM cola_member WHERE id=#{id} and pwd=#{pwd}
   </select>

</mapper>